type Workout
  @model
  @key(name: "byRoutine", fields: ["routineId", "id"])
  @key(name: "byUser", fields: ["userId"]) {
  id: ID!
  routineId: ID!
  userId: ID!
  name: String!
  notes: String
  status: WorkoutStatus
  score: String
  completedAt: String
}

enum WorkoutStatus {
  COMPLETE
  INCOMPLETE
}
