import * as React from "react";
import { SafeAreaView, Text, View } from "react-native";

const RoutineExercisesScreen = () => {
  return (
    <SafeAreaView>
      <Text>Routine Exercises</Text>
    </SafeAreaView>
  );
};

export default RoutineExercisesScreen;
